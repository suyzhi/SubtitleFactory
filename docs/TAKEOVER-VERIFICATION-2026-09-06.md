# 2026-09-06 接手复验

接手基线为 `5832227c2aa9b3f9792c14ffaab0c646c3d9fb04`，保留原有未提交优化并继续修复。当前交付是源码与本地开发服务，没有新建 App/DMG。历史文档中的安装包验收不能证明本轮源码已经过安装包验证。

## 本轮修复

- 保留独立项目库 → 项目工作区入口。编辑页提供自动、左右、上下布局，默认均分，支持拖动、25%–65% 比例滑块与比例记忆；切换布局和专注模式保留同一播放器。
- 字幕正文增大并换行，空译文列默认隐藏；收紧重复标题和操作区，修复窄播放器按钮文字重叠。样式页同时呈现预览与可滚动设置，导出页统一间距。
- 全局与项目任务面板统一活动状态。取消后持续轮询到 worker 退出；已结束的失败/取消转写可查看未完成草稿，明确采用后才写入正式字幕，并支持撤销。
- 修复候选详情重试刷新、取消草稿恢复、播放缓存失效及静态检查问题。保留本地 CPU 模型复用、媒体准备缓存与阶段调度；不改变默认识别参数，不静默切换设备。

## 验证与证据

本轮实测使用独立数据库、合成语音与视频，仅复用已安装模型；不修改用户项目，不向云端发送音视频或测试文字。

| 检查 | 结果 |
|---|---|
| 后端回归 | 216 项、25 子测试通过；1 条测试依赖弃用警告 |
| 前端回归 | 22 个文件、76 项通过 |
| 静态检查 | Ruff、Mypy 核心 28 文件、TypeScript、Oxlint 通过 |
| 前端生产构建 | 通过；两个新版 UI 标记存在，旧 AISettingsDialog 标记未进入 bundle |
| 真实 CPU 流程 | 导入、播放、转写、编辑保存、候选采用与撤销、SRT 和硬字幕 MP4 导出通过 |
| 缓存与重试 | CPU 模型与音频缓存实际命中；隔离媒体损坏后修复并从失败步骤重试成功 |
| 后台任务 | 长语音暂停、恢复，切换项目继续操作，再取消；17 条未完成草稿恢复并在 UI 采用 |
| Apple GPU | Qwen3-ASR 0.6B 显式 MLX 任务成功，device=Apple GPU (Metal)，fell_back=false |
| 布局 | 1400×900、1024×768、740×720 可见播放器与字幕；实际拖动 50%→55%，切换专注模式保持播放器节点 |
| 项目库 | 47 个隔离项目；第二页 UI 确认为 7 条，页码 2/2；搜索、紧凑视图与状态筛选已操作 |

[原始记录与截图](benchmarks/2026-09-06-takeover/)包括运行任务、暂停/恢复/取消结果、草稿、布局几何、构建与检查日志，以及本轮修改源码的 SHA-256。记录中的临时目录已匿名化。CPU/GPU 固定语料基准仍保留在历史报告中，不把接手前计时当作本轮加速结论。

## 明确边界

- 取消请求实测约 0.023 秒返回，当前识别片段退出约需 43.663 秒；不承诺立即停止推理。
- GPU 测试最初的断言未包含实际设备字符串 `Apple GPU (Metal)`，因此脚本断言曾失败；随后核对完整任务记录确认成功和无回退，未据脚本退出码误报推理失败。
- 本轮没有远程媒体下载、云端清洗/翻译实测，没有真人逐词边界标注，也没有小时级压力测试。
- Rust 源码未变，本轮没有重新编译 Rust 或验收安装包。前端构建后已清理规定的可重建产物，最终历史 App/DMG 保留。
- 当前开发服务在 `http://127.0.0.1:5173`，通过同源代理连接本地后端；重启前确认没有活动任务与启用的监听目录。
