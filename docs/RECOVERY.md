# 备份与故障恢复

字幕工厂默认保留 7 份每日备份和 4 份每周备份。可在“设置 → 备份与诊断”立即备份、恢复或打开备份目录。

数据库迁移前会生成独立安全备份。任一迁移步骤失败时，应用将恢复迁移前数据库并停止启动，避免使用部分升级的数据。

异常退出后，正在运行的工作流会记录为中断；具有恢复参数的自动字幕工作流会在下次启动重新排队，其他任务会在任务中心提供重试入口。字幕编辑历史和关闭自动保存时的草稿都跨重启保留。

模型下载中断后可在设置中重新准备或修复模型。磁盘不足时先释放空间再重试，不要删除项目数据库。需要反馈问题时导出脱敏诊断包。
